<%= provide(:title, 'All users') %>
<h1>All users</h1>

<% if signed_in? %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 52 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
<% else %>
<h2> Need to sign-in first </h2>
<% end %>
